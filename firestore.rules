rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Lock down access by default
      allow read: if true;  // Allow reads for now (change for production)
      allow write: if false; // Disallow writes by default
    }
  }
}